/**
 * Jest setup for ScamShield tests.
 * Ensures electron and electron-store are mocked before any imports that use them.
 */
import '@testing-library/jest-dom';

// Optional: extend expect (e.g. for custom matchers)
// expect.extend({ ... });
